# USACE-DEMO

Demonstration of modernizing an Oracle APEX application (modelled on the USACE
Enterprise Management Tool, EMT) into a FastAPI + React application while keeping
the Oracle database, built entirely on public data.

See `docs/` for the plan; the application lives in `api/`, `web/`, `db/` and `data/`.
